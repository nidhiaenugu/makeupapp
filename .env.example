# Optional. Point the app at a deployed AI proxy so users get the advisor
# without supplying their own Anthropic key. See docs/DEPLOYING_PROXY.md.
#
# Leave this unset and the app works fully — the AI features are simply off
# unless a user pastes their own key in the You tab.
EXPO_PUBLIC_AI_PROXY_URL=
